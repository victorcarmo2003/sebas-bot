{
  "name": "sebas-bot",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/panel",
    "packages/modules/changelog-roblox"
  ],
  "scripts": {
    "build": "npm run build --workspace=@sebas-bot/module-changelog-roblox && npm run build --workspace=@sebas-bot/core && npm run build --workspace=@sebas-bot/panel",
    "migrate": "npm run migrate --workspace=@sebas-bot/core"
  },
  "engines": {
    "node": ">=24"
  }
}
